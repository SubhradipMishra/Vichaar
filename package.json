{
  "name": "vichaar-root",
  "private": true,
  "scripts": {
    "install:all": "npm install && cd server && npm install && cd ../Vichaar && npm install",
    "dev:server": "cd server && npm run dev",
    "dev:frontend": "cd Vichaar && npm run dev",
    "build:server": "cd server && npm run build",
    "build:frontend": "cd Vichaar && npm run build"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
